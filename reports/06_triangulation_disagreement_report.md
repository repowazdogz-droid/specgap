# SpecGap Divergence Report — Partial read-only root vs general write permission

> SpecGap does not prove the spec is correct. It identifies likely semantic divergence zones and checks extracted constraints (via Z3 over the abstract sandbox model) for consistency or implication failure.

## Summary

- **Semantic divergence detected.**
- Semantic divergences found: 0 (0 high structural severity).
- Formal implication checks failed: 2 of 2.
- Internally inconsistent layers: 0.
- Triangulation: **disagreement** between structural diff and Z3 on at least one layer (see Triangulation Summary).

## Triangulation Summary

Structural divergence analysis and Z3 implication checking **operate independently** over extracted constraints. **Disagreement is preserved** because the two mechanisms capture different abstraction-level properties (name-level weakening lattice vs propositional sandbox model). Do not treat agreement as a merged verdict — see `docs/TCB.md`, `docs/ENCODING.md`.

| Layer | Structural | Z3 implication | Agreement | Counterexample |
| --- | --- | --- | --- | --- |
| Formalized Policy | no_divergence_detected | fails | **no** | yes |
| Implementation Claim | no_divergence_detected | fails | **no** | yes |

_At least one layer shows **disagreement** between structural diff and Z3. Treat both signals as heterogeneous evidence from independent mechanisms; do not collapse to a single verdict._

**TCB scope for this triangulation:**
- rule-based constraint extraction (fixed phrase vocabulary)
- hand-authored WEAKER_OF structural weakening lattice
- abstract propositional sandbox model (docs/ENCODING.md)
- Z3 quantifier-free satisfiability (z3-solver)

## Inputs

- **Stakeholder Intent:** The root filesystem is read-only per container policy.
- **Formalized Policy:** Write access to /var is allowed for application logs and runtime state.
- **Implementation Claim:** Write access to /var is allowed for application logs and runtime state.

## Human Annotation (outside tool evaluation)

> **HUMAN NOTE**
> This annotation is explanatory context supplied by the example author.
> It is not generated by SpecGap.
>
> Structural diff does not flag this pair: intent extracts partial readonly_root_fs (not in the STRICT weakening lattice), while Z3 finds that write_allowed permits fs_write outside /tmp — violating the intent's partial read-only encoding. Triangulation agreement is no: structural silent, Z3 fails.


## Extracted Constraints

_Extraction mode: `rule` — deterministic rule-based parsing only. Fuzzy/paraphrase-aware extraction is available via `--extractor fuzzy`._

**Stakeholder Intent**
- `readonly_root_fs` — Root filesystem read-only (writes possible elsewhere) [filesystem/partial, via rule]

**Formalized Policy**
- `write_allowed` — Writes allowed to /var [filesystem/permission, via rule]

**Implementation Claim**
- `write_allowed` — Writes allowed to /var [filesystem/permission, via rule]

## Semantic Divergence

| Category | Stakeholder Intent | Formalized Policy | Implementation Claim |
| --- | --- | --- | --- |
| filesystem | Root filesystem read-only (writes possible elsewhere) | Writes allowed to /var | Writes allowed to /var |

_No structural divergences detected._

## Z3 Formal Check

Each check asks Z3 whether one layer's constraints **imply** another's. A `sat` result for the negated query means a permitted-but-forbidden behavior exists — a concrete implication failure in the abstract sandbox model.

### Formalized Policy ⇒ Stakeholder Intent

- **Result: implication FAILS.** Z3 returned `sat` for the negation: there is a behavior permitted by Formalized Policy that Stakeholder Intent forbids.
- Violated target constraint(s): `readonly_root_fs`

Counterexample behavior:
- `fs_write = true` — a filesystem write occurs
- `write_other = true` — the write target is outside /tmp

Therefore **Formalized Policy** permits a behavior that **Stakeholder Intent** appears to forbid.

### Implementation Claim ⇒ Stakeholder Intent

- **Result: implication FAILS.** Z3 returned `sat` for the negation: there is a behavior permitted by Implementation Claim that Stakeholder Intent forbids.
- Violated target constraint(s): `readonly_root_fs`

Counterexample behavior:
- `fs_write = true` — a filesystem write occurs
- `write_other = true` — the write target is outside /tmp

Therefore **Implementation Claim** permits a behavior that **Stakeholder Intent** appears to forbid.

Internal consistency (each layer satisfiable on its own):

- Stakeholder Intent: consistent (`z3: sat`)
- Formalized Policy: consistent (`z3: sat`)
- Implementation Claim: consistent (`z3: sat`)

## Counterexample

For **Formalized Policy ⇒ Stakeholder Intent**:
- `fs_write = true` — a filesystem write occurs
- `write_other = true` — the write target is outside /tmp

For **Implementation Claim ⇒ Stakeholder Intent**:
- `fs_write = true` — a filesystem write occurs
- `write_other = true` — the write target is outside /tmp

## Human-Readable Explanation

SpecGap extracted the constraints stated in each layer and asked Z3 whether the downstream layers still satisfy the stakeholder intent. They do not. Formalized Policy permits a behavior — shown in the counterexample above — that the stakeholder intent forbids.

In plain terms: the wording drifted between what was *asked for*, what was *formalized*, and what was *claimed to be implemented*. A reviewer reading only one layer would not see the gap. The proof obligation was quietly redefined.

## Limitations

- SpecGap does not prove the spec is correct. It identifies likely semantic divergence zones and checks extracted constraints (via Z3 over the abstract sandbox model) for consistency or implication failure.
- Constraint extraction is rule-based and recognizes a fixed vocabulary of sandbox constraints. Unrecognized phrasing is silently not extracted.
- Fuzzy / paraphrase-aware extraction (`--extractor fuzzy`) is opt-in and rule-first; fuzzy-extracted constraints are flagged `requires_human_review` and are not authoritative. The default `rule` mode is fully deterministic and offline.
- The Z3 model is a small propositional abstraction of sandbox behavior, not a model of a real kernel or runtime.
- Counterexamples are illustrative behaviors, not exploit proofs.
- A clean report means *no divergence was detected*, not that the specification is correct.

